^D已创建 `calculator.py`，包含以下功能：

`★ Insight ─────────────────────────────────────`

**文档字符串规范**
- 使用 Google 风格的 docstring，包含 Args、Returns、Raises 三个部分
- 模块级 docstring 描述整个模块的用途

**类型提示 (Type Hints)**
- `a: float, b: float -> float` 提供静态类型检查支持
- 提高 IDE 自动补全和代码可读性

**错误处理**
- 除法函数显式检查除零并抛出有意义的错误信息
- 主循环使用 try/except 捕获用户输入错误
`─────────────────────────────────────────────────`

**文件结构：**
| 函数 | 功能 |
|------|------|
| `add(a, b)` | 加法 |
| `subtract(a, b)` | 减法 |
| `multiply(a, b)` | 乘法 |
| `divide(a, b)` | 除法（含零检查）|
| `main()` | 交互式命令行界面 |

**运行方式：**
```bash
python calculator.py
```

然后按提示输入如 `1 + 2` 或 `10 / 3` 进行计算。
[<u[?1004l[?2004l[?25h]9;4;0;[?25h