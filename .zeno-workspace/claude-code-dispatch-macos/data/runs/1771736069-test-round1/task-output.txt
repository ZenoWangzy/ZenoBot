^D已添加 `greeting` 函数。

`★ Insight ─────────────────────────────────────`
- **模块一致性**：新函数遵循现有代码风格，使用相同的文档字符串格式（Google 风格）
- **位置选择**：放在 `add` 函数之前，作为工具函数而非计算功能，逻辑分组清晰
- **返回值 vs 打印**：选择返回字符串而非直接 `print()`，使函数更易于测试和复用
`─────────────────────────────────────────────────`

新函数位于 `calculator.py:7-15`，调用方式：

```python
from calculator import greeting
print(greeting())  # 输出: Hello World
```
[<u[?1004l[?2004l[?25h]9;4;0;[?25h