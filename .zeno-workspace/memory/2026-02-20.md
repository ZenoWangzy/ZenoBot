# 2026-02-20

## 用户工作流与偏好（新增）

- 用户明确要求：默认作为 **supervisor** 角色，执行层优先委派给 **Claude Code CLI（cc）**，而不是本地亲自执行。
- 用户新增派发规则：
  - 文本/调研任务 → **Gemini CLI**
  - 开发/编码/自动化实现 → **Claude Code CLI**
  - Bug 修复 → **Codex CLI**
- 用户强调开发规范：**TDD（先测试后开发）**，并且“文档和测试最重要”。

## OpenClaw/CC 工程进展

- 创建并迭代了 `superpowers-aware-dispatch` skill（含 prompt 模板与 dispatch 脚本）。
- 新建并发布开源仓库：
  - `ZenoWangzy/openclaw-cc-superpowers-kit`
  - 已补充中英文双语文档：README / CLAW / EXAMPLES
  - 已补充安装脚本：`scripts/install.sh`、`scripts/install.ps1`
  - 发布了 `v0.1.0` release。

## 关键故障与定位结论

- 多次出现“任务显示 running 但无产物/无回调完成”的问题。
- 排查中发现 dispatch 脚本曾有关键拼写错误：`nohup` 被写成 `ohup`，已修复并提交。
- 进一步 step-by-step 测试发现：
  - 直连 `claude -p`（前台）可正常完成并写文件；
  - 后台 dispatch 场景仍有“无输出卡住”现象。
- 当前判断：CC 在**无 TTY/后台场景**存在卡死风险，需引入 PTY 包装执行链路（例如 wrapper/script 方案）。

## 自动化可靠性策略（确定方向）

- 已加 watchdog 思路：超时、卡住、API错误、非0退出告警。
- 已加 AskUserQuestion 拦截与 wake 通知思路（防止无人值守任务卡在提问阶段）。
- 明确后续优化方向：
  - 任务级 run_id 隔离（避免 latest 单槽覆盖）
  - 交付物校验门禁（缺文件即失败）
  - 状态机式回调（started/running/completed/failed 等）

## 远程使用场景（用户目标）

- 用户要求“出门手机 Discord 发消息时，Mac 持续可工作”。
- 已新增并提交 work mode 脚本：
  - `scripts/workmode-on.sh`
  - `scripts/workmode-off.sh`
- 目标状态：不合盖、接电、允许黑屏但不睡眠不断网。

## 2026-02-20 18:44 补充：可靠性排障进展

- 用户要求采用“step by step”排障法：每加一层规则就做一次 smoke test，定位具体坏链路。
- 已验证：
  - 前台直连 `claude -p` 可成功写文件（`cc-direct-smoke.txt = CC_OK`）。
  - 后台 dispatch 场景仍出现“running/stop 信号存在，但无输出文件、无交付物”的问题。
- 本轮关键结论：
  - 当前问题主因不在 Claude Code 本体，而在 dispatch + hook + 无TTY后台执行链路。
  - “完成回调 ≠ 交付完成”仍是核心风险，必须引入交付物门禁。
- 尝试执行 Step 3（PTY wrapper 改造）任务 `cc-step3-pty-fix`：
  - 状态回调显示 done（Stop），但未生成要求交付物（`pty-step3-report.md`、`test-dispatch-pty.sh` 均缺失）。
  - 再次证明现有管道存在“假完成”问题。
- 用户新增流程要求：需要定期可见性反馈（提出“每10分钟查看一次”）。
