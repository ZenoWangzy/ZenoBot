# 2026-03-07

- [preference][high] 用户明确纠正：凡是 CC/Codex 任务的 5 分钟进度检查，不能只做 internal handling；应向 Discord 外发可见进度汇报。此前因系统提醒文案含“Handle internally”而未外发，被用户指出。
- [workflow][high] 今天为网页抓取新增本机 Scrapling 能力：
  - venv: `/Users/ZenoWang/.openclaw/workspace/.venvs/scrapling`
  - skill: `/Users/ZenoWang/.openclaw/skills/scrapling/SKILL.md`
  - runner: `/Users/ZenoWang/.openclaw/skills/scrapling/scripts/scrapling_fetch.py`
  - Chromium 已安装用于 dynamic 模式。
- [environment][medium] Scrapling 在这台机器上 static fetch 默认会遇到 TLS 证书链问题；runner 已做 verified→insecure fallback，并输出 warning。动态抓取走 Playwright Chromium。
- [task][medium] 正在用 Claude Code CLI 修订 OpenClaw 文档：
  - `/Users/ZenoWang/Documents/project/openclaw/docs/plans/2026-03-07-cc-auto-recover-design.md`
  - `/Users/ZenoWang/Documents/project/openclaw/docs/plans/2026-03-07-cc-auto-recover-impl.md`
    重点修正：路径统一、tmux 依赖降级、lease 并发语义、re_dispatch 预算粒度、safe reentrant 判定、duplicate 压制规则。
- [memory][high] 用户新增铁律：凡是 CC/Codex 任务的 5 分钟进度提醒，一律对 Discord 外发，不走 internal-only 理解。
- [memory][high] 用户还要求遵守“记忆插件铁律”：遇到坑/教训时，应立即做双层记忆存储（technical + principle）、短小原子、先 recall 再 retry、改 memory plugins 前确认目标仓库、改 `plugins/*.ts` 后清 `/tmp/jiti/` 再重启 gateway。当前会话无 memory plugin 工具可直接调用，因此先写入文件记忆作为持久兜底。

---

## 2026-03-07 晚间补充

- [bug][critical] `watch-cc-task.sh` 在顶层 while 循环误用 `local` 关键字，导致运行时报错 `local: can only be used in a function`，watchdog 悄悄死掉，状态无法收敛。**教训：Bash 的 `local` 只能在函数内使用，`bash -n` 语法检查查不出这个运行时错误。**
- [workflow][high] CC CLI dispatch 链路修复验证通过：成功/失败两条路径的 watchdog 都能正确收敛状态（`done`/`failed`），通知标志（`completion_sent`/`failure_sent`）正确更新。
- [rule][high] 新增强规则：**派发 CC CLI / Codex CLI 异步任务时，必须挂 watcher，每 5 分钟检查并汇报。** 已写入 USER.md。
- [lesson][high] 不要轻信 `progress.md` 或任何文档的自述。**必须读实际代码、跑实际测试。** 文档说“集成完成”不等于真的完成。
